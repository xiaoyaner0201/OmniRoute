---
ts: 20260825-082000
agent: leader
action: intake + 风险分级 + dispatch BEHAVIOR_DISCOVERY
tree: —
verdict: —
---

## Intake

Issue：HD-34 `73755f20-29f4-42fd-b665-193ab8cc217d`。
仓库 `xiaoyaner0201/OmniRoute`（上游 `diegosouzapw/OmniRoute`）。

### 分支与基线（机械核对，非语义判断）

- 内部交付分支：`multica/hd-34-analytics-overview-estimates`
- Base commit：`d5a8861cd6d8173ed460d953db9426277c6fb01f`
  （`fix/11459-cc-cost-estimates` tip，与 Issue 描述一致）
- 该 base 的 merge-base 与 `release/v3.8.51` = `d82b68274c75c14d258b4898a34edc25d9712b87`，
  即 Issue 所述 Draft PR #11460 的 base，一致。
- `fix/11459-cc-cost-estimates` 相对 `release/v3.8.51` 的既有改动面：
  `src/app/api/usage/analytics/route.ts`、`src/lib/db/usageAnalytics.ts`、
  `src/lib/db/usageAnalytics/sources.ts`、`src/lib/usage/aggregateHistory.ts`、
  `src/lib/usage/usageStats.ts`、`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`、
  `changelog.d/fixes/11459-claude-code-cost-estimates.md` 及两个测试文件。

### Issue 声称行为的机械落点核对

仅核对 path 是否存在、grep 是否命中，不做语义裁决：

- `src/shared/components/UsageAnalytics.tsx` 存在，由
  `src/app/(dashboard)/dashboard/analytics/page.tsx:132` 渲染。
- 该组件在 `UsageAnalytics.tsx:55-64` 组装 `URLSearchParams` 并 fetch
  `/api/usage/analytics`，参数集合为 `range` / `startDate` / `endDate` / `apiKeyIds`；
  `grep includeFlatRateEstimates` 在该文件 **0 命中**。
- `src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx:347` 显式传
  `includeFlatRateEstimates: "true"`。
- `src/app/api/usage/analytics/route.ts:364` 读取该参数，并在 583/622/694/710/773/818/828/947
  多处以 `!includeFlatRateEstimates` 分支控制口径，909 行回写
  `includesFlatRateEstimates`。

以上仅确认 Issue 描述的入口真实存在，**不构成行为面闭包**——闭包由 Discovery inventory
与 Planner 建模负责。

## 风险分级：`HIGH_RISK`

命中触发器：**资金 / 成本口径面**。本次改动改变面向用户的成本金额呈现语义
（Issue 记录的生产 live 30d 证据：同一批数据由 `$0` 变为 `3961.107051`），
且该 exact tree 将作为上游 Draft PR 的同步来源。按协议 Agent 不得自行降级。

未命中但需 Planner 显式 disposition 的邻接面：API-key filter 与 range 参数组合、
`byApiKey` / `byAccount` 等分组口径是否随该参数漂移。

由此激活的强制要求：

1. Plan 之前先跑独立 `BEHAVIOR_DISCOVERY` Run，生产 `behavior-inventory.v1`；
   `produced_by_run_id` / `produced_by_context_id` 必须 ≠ Plan Run/context。
2. Plan 中必须提名 2–3 个具名 lens（含至少一位 `dissent_role: true`）。
3. QA 拆 blind Charter 与 execution 两 Run。
4. Review 拆 `BLIND_BEHAVIOR_AUDIT` 与 `EVIDENCE_CHALLENGE` 两 Run。
5. Gate receipt 必须绑定真实 Run ID、artifact/inventory digest、exact tree、validator hash。

## Dispatch

第一棒派给 **Coder（实现）** 执行 `BEHAVIOR_DISCOVERY`，而非 Planner，理由是
协议要求 inventory 生产方非 Planner；同时避免占用 QA / Reviewer 的 blind lane，
保住其后续独立性。Coder 后续按冻结 Plan 实现，本次 discovery 先验不影响任何 blind Gate。

Gate bundle 已复制到本容器 `/workspaces/HD-34/gates/`（`$HOME` 与 `/tmp` 为
noexec / 非持久，不从其运行）。

## 边界

工程链终点是 exact subject tree READY。工程 Agent 不 push 公开分支
`fix/11459-cc-cost-estimates`、不更新 Draft PR #11460、不签 CLA、不改写作者、
不部署生产。内部交付分支仅用于 ledger 与跨 Agent 交接。

## 交接

- 状态：INTAKE 完成，`HIGH_RISK`
- 产物：分支 `multica/hd-34-analytics-overview-estimates`，base `d5a8861c`，ledger 骨架已建
- 下一步：Coder 执行 `BEHAVIOR_DISCOVERY`，产出 `behavior-inventory.v1` 并冻结
- 未解决：inventory 未产出，Plan 未启动
- 需要决策：无
